title: "Hello World"
description: "Prints Hello World in parallel ways"

steps:
  - label: "Say Hello"
    tasks:
      - label: "Hello English"
        uses: shell
        with:
          script: echo "Hello, World!"